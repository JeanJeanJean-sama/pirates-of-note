# Pirates of note（Pon） v0.5.0 — β版

自分のnoteのアクセス数値を記録・分析する**非公式**ツールです。

noteのアクセス数値を毎日自動で記録し、推移を見える化するChrome拡張機能です。取得するのは**自分のアカウントのデータだけ**です。
未返信のコメントや、自分が他の人の記事に書いたコメントも記録します。

> note株式会社とは関係のない非公式ツールです。noteの仕様が変わると動かなくなることがあります。

## できること（無料版）

| 機能 | 内容 |
|---|---|
| 毎日の自動記録 | noteを開いたときに1日1回、全記事の**全期間の累計**（インプレッション・PV・スキ・コメント・売上）とフォロワー数を記録 |
| 推移グラフ | 前回の記録から増えた数と累計を、指標ごとに表示（使い始めた日から記録がたまる） |
| 前回から伸びた記事 | PVが増えた順に上位10記事を表示 |
| 記事カード | 記事ごとに「入口（IMP→PV）」「反応（PV→スキ）」を棒で比較。全記事の中央値の目印、前回比、開封率・スキ率、記事ごとの推移 |
| マップ | 全記事を散布図（両対数）で表示。A：IMP→PV、B：PV→スキ。直近30日の記事を色分け、開封率・スキ率の目安線つき |
| 記事一覧（表） | 並べ替えと絞り込みができる表。前回比、開封率（PV÷IMP）、スキ率（スキ÷PV）つき |
| 未返信コメント | 自分の記事についたコメントのうち、まだ返信していないものを一覧表示。未返信の件数はアイコンに数字で表示。「対応済み」にもできる |
| 返信への返信 | 自分が返信したコメントに、相手からさらに返信が来たもの（通知から検出。要確認として表示） |
| 自分がコメントした記事 | 他の人の記事を開いたときにコメント欄から、また通知（自分のコメントへの返信・スキ）から記録 |
| 本文の保存 | 自分の記事の本文を保存してCSVを自動でダウンロード（直近7日の新しい記事は自動、それ以前はボタンでまとめて）。CSV（GAS版の記事一覧シートと同じ列順）とMarkdown（ZIP）で書き出し |
| 書き出し | 最新データまたは全記録をCSVで書き出し。JSONでバックアップと復元（本文も含む） |

## フォロー特典：称号と着せ替え

作者 ジァン=サマー（[@jeanjeanjean](https://note.com/jeanjeanjean)）のnoteをフォローすると、「称号・着せ替え」タブが使えます。ほかの機能はフォローしなくてもすべて使えます。

| 特典 | 内容 |
|---|---|
| 称号 | 海賊・海軍・海の住人の肩書（見習い水夫〜伝説の海賊、海軍二等兵〜元帥など）から選ぶか、16文字までの好きな肩書を自由に名乗れる。画面上部の名前の前と、手配書に表示 |
| 着せ替え | 海賊船（羊皮紙と木の甲板）、海軍本部（白と紺）。ダークモードでは暗い配色に切り替わる |
| 特別な称号 | 彼の記事への スキ（1・5・全記事）／コメント（1・3記事）／引用（1・3記事）で解放。スキ・コメント・引用がそろうと「ジァン=サマー海賊団 右腕」と着せ替え「黄金の宝島」 |
| 海賊王 | @jeanjeanjean 本人だけが名乗れる称号と、専用の着せ替え「海賊王の旗艦」 |

確認のしかた：フォローしていない間は1日1回（通信1回）、フォロー中は週1回、フォロー・スキ・コメントを確認します。引用は、自分の記事の本文に彼の記事のURL（埋め込み）があるかで判定し、記録のたびに最大15記事ずつ確認します。結果はブラウザ内にだけ記録します。

## スマホ・Web版（Pon Web）

スマホや、Chrome以外のブラウザでも使えるWeb版です。インストール不要で、ブックマークレット「Ponで記録」で記録します。

- **Web版を開く**：https://jeanjeanjean-sama.github.io/pirates-of-note/
- **登録のしかた**：https://jeanjeanjean-sama.github.io/pirates-of-note/install.html

データはそのブラウザの中にだけ保存され、外部には送りません。拡張機能版とWeb版のバックアップ（JSON）は、互いに読み込めます。

## ダウンロード（拡張機能版）

[Releases](https://github.com/jeanjeanjean-sama/pirates-of-note/releases) から最新の `pon-beta-vX.Y.Z.zip` をダウンロードしてください。

## インストール（β版）

1. ZIPを展開し、中の `pirates-of-note` フォルダを消さない場所に保存します。
2. Chromeで `chrome://extensions` を開き、右上の「デベロッパーモード」をオンにします。
3. 「パッケージ化されていない拡張機能を読み込む」を押して、このフォルダを選びます。
4. noteにログインした状態で `https://note.com/` を開きます。数十秒後に最初の記録が保存されます。
5. ツールバーのアイコンを押すと、ダッシュボードが開きます。

## 仕組みと安全性

- noteとの通信は、**noteのページ内（content script）でだけ**行います。
- 認証には、noteがブラウザに用意するCookie `note_gql_auth_token` を使います。拡張機能が自分で発行したり、保存したり、外部に送ったりすることはありません。
- noteへのリクエストは**1秒以上の間隔**をあけて送ります。
- 取得するタイミング
  - 全期間の記録：1日1回
  - 未返信コメントの確認：6時間ごと。コメント数が変わった記事だけを確認します
  - 通知の確認：12時間ごと
- 保存先は、このブラウザの拡張機能用の保存領域（IndexedDB）だけです。外部のサーバーには何も送りません。
- 使う権限は `storage`、`unlimitedStorage`、`https://note.com/*` の3つだけです。

## 使っている note の内部API（非公式）

| 用途 | API |
|---|---|
| 記事別の全期間の数値 | `POST https://graphql.note.com/graphql`（`dashboardNoteListConnection`, unit=ALL） |
| 自分のID・フォロワー数 | `GET /api/v2/current_user`（ID・表示名・フォロワー数以外は保存しません） |
| コメント一覧・返信済みかどうか | `GET /api/v3/notes/{key}/note_comments`（`is_creator_replied`。公開情報のため、ダッシュボードからの再確認はログイン情報なしで行う） |
| 通知 | `GET /api/v3/notices`（最大25ページ、約300件） |
| 記事の本文 | `GET /api/v3/notes/{key}`（noteのページ内・ログイン状態で取得） |
| フォロー特典の確認 | `GET /api/v2/creators/jeanjeanjean`（`isFollowing`）、`GET /api/v2/creators/jeanjeanjean/contents`（`isLiked`）、彼の記事の `note_comments` |

## ファイル構成

```
manifest.json
icons/            アイコン
src/background.js 保存・ロック・バッジ（noteとは通信しない）
src/content.js    noteのページ内で動く収集処理
src/db.js         IndexedDB
src/dashboard.*   ダッシュボード画面
src/views.js      記事カード・マップ・未返信の再確認
src/bodies.js     本文の保存状況・書き出し（CSV / Markdown ZIP）
src/store.js      保存処理（拡張機能とWeb版で共通）
src/perk-collect.js フォロー特典の確認（noteのページ内。ブックマークレットにも同梱）
src/perks.js      称号・着せ替えの画面
src/theme-boot.js 着せ替えを画面表示前に当てる
PRIVACY.md        プライバシーポリシー（ストア申請用）
```

## 更新履歴

- v0.5.0：フォロー特典「称号・着せ替え」を追加（海賊船・海軍本部・黄金の宝島・海賊王の旗艦、手配書、特別な称号）。新しく公開した記事の本文が自動で取り込まれていなかった問題を修正。画面の一番下にバージョンを表示。ダウンロードするファイル名を「pon-」で始まる名前に統一（pon-backup / pon-stats / pon-bodies）。ブラウザ内の保存場所の名前を変更（v0.4.2 以前の記録は自動で引き継ぎ、古い名前のバックアップも復元できる）
- v0.4.2：「記録（Ponの中）」と「⬇ ダウンロード（パソコンのファイル）」の言葉と見た目を統一。記事カードは常に「⬇ 本文をダウンロード（CSV）」、「全記事の本文をダウンロード」は足りない分を取り込んでから全記事分をダウンロード。ダウンロード後にファイル名と保存先を表示
- v0.4.1：本文の保存をダッシュボードから直接行うように変更（noteのタブを開かない）。進み具合を表示し、終わったらCSVを自動でダウンロード。記事カードの「本文を保存」はその記事のCSVをすぐにダウンロード
- v0.4.0：スマホ・Web版（GitHub Pages）を追加。保存処理を store.js にまとめ、拡張機能とWeb版で共通化
- v0.3.0：記事本文の保存と書き出し（CSV・Markdown ZIP）を追加。記事カードに「本文を保存」ボタン
- v0.2.0：記事カード表示とマップ（散布図）を追加。返信した未返信コメントが、ダッシュボードを開いたときや記事ページで30秒ほど待つと、すぐ消えるように改善。アイコンを錨に変更
- v0.1.1：コメントの確認が1回60記事で止まり、残りが6時間後まで確認されなかった問題を修正（次にnoteを開いたときに続きを確認）。「返信への返信」の検出を追加。確認の進み具合を表示
- v0.1.0：最初の版

## 既知の制限

- 無料版は「全期間の累計」を毎日記録し、その差から日ごとの増えた数を出します。記録しなかった日があると、その区間は数日分がまとめて1点になります。
- 自分のコメントのうち、拡張機能を入れる前に書いたものは、その記事を開き直すか、返信・スキの通知が届いたときに追加されます。
- 「今すぐ取得」でnoteのタブを裏で開いたときは、Chromeの省電力機能で処理が遅くなることがあります。

## 不具合報告・要望

[Issues](https://github.com/jeanjeanjean-sama/pirates-of-note/issues)、または作者のnoteまでお寄せください。

## ライセンス

[MIT License](LICENSE)
